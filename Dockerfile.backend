FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl \
    && rm -rf /var/lib/apt/lists/*

COPY backend/pyproject.toml ./pyproject.toml

RUN pip install --no-cache-dir --index-url https://download.pytorch.org/whl/cpu "torch>=2.2.0"

RUN pip install --no-cache-dir -e ".[dev,training,cloud]"

COPY backend/ ./

RUN mkdir -p /app/data /app/projects

ENV DATABASE_URL=sqlite+aiosqlite:///app/data/workbench.db
ENV PROJECTS_DIR=/app/projects
ENV DATA_DIR=/app/data

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
