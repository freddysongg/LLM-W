import { describe, it } from "vitest";

/* TODO(step3): wire this test after the swap-in logic lands -- remove TODO when the test is implemented */
describe.skip("useVoiceSession transcript interim swap-in", () => {
  it.todo("replaces trailing interim entry when finalized arrives");
});
